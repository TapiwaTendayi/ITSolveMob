# ITSolve Frontend — Environment Variables
# For a local internal server deployment, this file is NOT needed.
# The frontend and backend run on the same server behind nginx.
#
# Only create a .env file if you are running the frontend on a
# DIFFERENT machine from the backend during development.
# In that case, this file is not used — leave it empty.
